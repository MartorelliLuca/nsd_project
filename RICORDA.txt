



________________________________________________________________________________________________________

Secondo terminale ebpf-1

watch -n 1 '
echo "===== identity_map ====="
bpftool map dump pinned /sys/fs/bpf/identity_map 2>&1
echo
echo "===== auth_map ====="
bpftool map dump pinned /sys/fs/bpf/auth_map 2>&1
'

bridge vlan show

________________________________________________________________________________________________________
#per vedere i 3 parser:
bpftool net show
bpftool prog show | grep -E 'xdp_radius_parser|xdp_eap_parser|xdp_dispatcher'

"bpftool net show dimostra che XDP è agganciato in modalità driver alle tre interfacce.
Siccome il loader usa libxdp, sulle interfacce compare un dispatcher. Il secondo comando
dimostra che dietro i dispatcher sono caricati i miei programmi (ext): un parser RADIUS 
su eth0 e due istanze del parser EAPOL, una per eth1/B1 e una per eth2/B2."
________________________________________________________________________________________________________

chmod +x supplicant.sh
./supplicant.sh


________________________________________________________________________________________________________


Ricorda per client-A1:
sudo ip route del default
sudo ip route replace default via 192.168.1.1 dev enp0s3